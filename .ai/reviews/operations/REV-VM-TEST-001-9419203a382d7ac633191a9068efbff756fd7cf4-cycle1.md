# Review: VM-TEST-001 harness (a)

Ticket: VM-TEST-001
Reviewer:                 REVIEWER AI (sole gatekeeper for all backend + frontend code)
Model/tool used:          Muse Spark / git collection (no implementation edits)
Commit reviewed:          9419203a382d7ac633191a9068efbff756fd7cf4
Review cycle number:      1
Files reviewed:
- backend/vmarket-web/tests/Feature/DumpSchemaTestCase.php (new, 513 lines, collected via `git show backend/VM-TEST-001:<path>`)
- backend/vmarket-web/tests/Feature/DeliveryFlowLifecycleTest.php (extends TestCase -> DumpSchemaTestCase + require_once)
- backend/vmarket-web/tests/Feature/ExampleTest.php (same harness switch)
Tests run by reviewer:    none this cycle (no BACKEND_DONE, no runner log attached to branch; runner required before APPROVED)

Business-rule findings:   none — test-harness only, no business logic touched. In scope per work order.
Security findings:        none in harness diff. No secrets, no prod DB touch per diff.
Prompt-injection / untrusted-input findings: none.
Frontend findings:        none.
Backend findings:
- Approach is plausible: MySQL install-dump -> SQLite :memory: conversion (strip CHARACTER SET/COLLATE/COMMENT/ON UPDATE/ENGINE) because migration chain lacks base CREATEs. Consistent with BASELINE LD-006.
- Concern: `require_once __DIR__ . '/DumpSchemaTestCase.php'` workaround for vendor-junction autoload gap is environment-specific; must prove it passes on clean runner (`scripts/tests/run-backend-tests.ps1`, Herd php84) not just local junction layout.
- Concern: branch base is stale (c55a74ef-era; v1 now 3976e8aa with launch tickets). Harness must rebase cleanly onto current v1.
Integration findings:     branch also contains unrelated product commit 6a8fb3b3 (see VM-ASSETS-001 ruling), now verified pushed to origin/backend/VM-TEST-001 — must be reverted/stripped from this ticket before merge. `git diff --stat v1..backend/VM-TEST-001` currently mixes harness + product + v1-drift.
Client compatibility findings: none.
Testing findings (blockers):
- Missing: full `scripts/tests/run-backend-tests.ps1` log showing DeliveryFlowLifecycleTest + ExampleTest PASS and invariants/security still green.
- Missing: `php -l` evidence on touched test files.
- Missing: BACKEND_DONE + filled ticket notes (contract/schemas/migration notes) in branch ticket file (branch copy still shows Blocked: yes on old PHP env).
Performance findings:     none (test-only).
Dependency findings:      none.
Privacy / data-impact findings: none.

Blockers:
1. Attach runner log + php -l; declare BACKEND_DONE in ticket History on the branch.
2. Revert/strip product files (InstallController, UpdateController, filesystems.php / commit 6a8fb3b3, now on origin) from backend/VM-TEST-001 — they belong to VM-ASSETS-001. `git diff --stat` must show only test-harness files.
3. Rebase/refresh onto current v1 (3976e8aa) so the diff no longer inverts launch-ticket files.
Non-blockers:
- Consider documenting dump-source pin (installation/backup/database.sql version) so future dump drift is detectable.

Decision:                 CHANGES_REQUIRED
